
#include <stdio.h>
#include "abbp.h"
#include "hevc_parser.h"
#include <math.h>

void hevc_parser_init(const char *file_path) {
    printf("%s\n", file_path);
}

int int_sqrt(int x) {
  return sqrt(x);
}